{
  "name": "the-anvil-loop",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "A loop that climbs on the artifact, and instruments that watch the climb without standing in front of it.",
  "engines": { "node": ">=18" },
  "scripts": {
    "doctor": "node tools/doctor.mjs",
    "board": "node tools/board.mjs",
    "instruments": "node tools/instruments.mjs",
    "validate": "node tools/validate-spec.mjs",
    "journal": "node tools/journal.mjs",
    "status": "node tools/status.mjs"
  },
  "_comment": [
    "The six shipped programs use only the Node standard library and need no install.",
    "The framework must run the moment the repo is cloned, before any decision about",
    "your stack has been made.",
    "",
    "The generated instruments WILL need dependencies — a headless browser driver, an",
    "image decoder, whatever your engine requires. They are added here when the",
    "instrument that needs them is built, which is when its reading becomes useful."
  ],
  "dependencies": {},
  "devDependencies": {}
}
